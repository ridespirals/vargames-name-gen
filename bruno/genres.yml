info:
  name: genres
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/genres"
  headers:
    - name: Client-Id
      value: "{{client_id}}"
    - name: Authorization
      value: Bearer {{access_token}}
    - name: Body
      value: fields *;
  body:
    type: text
    data: fields *;
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
