info:
  name: alternative_names
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/alternative_names"
  headers:
    - name: Client-Id
      value: "{{client_id}}"
    - name: Authorization
      value: Bearer {{access_token}}
    - name: Body
      value: fields *;
  body:
    type: text
    data: |-
      fields *, game.name;
      offset 195856;
      limit 5;
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
